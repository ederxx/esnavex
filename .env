# Supabase env vars removed — migration to Firebase completed.
# If you need to export data from Supabase later, temporarily set SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY in your shell before running `scripts/firebase/exportFromSupabase.cjs`.
